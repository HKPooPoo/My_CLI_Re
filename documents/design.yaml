

html:
  - blackboard:
    - log:
      - <button>:
        - push:
          - tailIndex++
          - if tailIndex = 9 -> delete index[0] then tailIndex++
        - pull:
          - headIndex--
          - headIndex range = (0, tailIndex)
      - <textarea>, allow user to input
      - the content is instantly saved in localStorage
      - max slot = 10
      - FIFO
    - todo:
      - <button>:
        - push:
          - tailIndex++
          - if tailIndex = 9 -> delete index[0] then tailIndex++
        - pull:
          - headIndex--
          - headIndex range = (0, tailIndex)
      - <textarea>, allow user to input
      - the content is instantly saved in localStorage
      - max slot = 10
      - FIFO
      - headIndex variable indicates which page to show
    - misc:
      - account page:
        - <input>:
          - email: optional
          - uid: only limitation:/ maxLength = 32 
          - passcode: only limitation:/ maxLength = 32 
        - <button>:
          - register: 
            - must have filled in uid and passcode
            - uid verification to prevent duplicates
          - login:
            - verify uid and passcode
            - allow access server database for its data
          - logout: logout
      - data control feature:
        - <button>:
          - checkout:
            - only functional on login
            - fetch and overwrite localStorage for blackboard from server
          - commit:
            - only functional on login
            - overwrite data with localStorage to server
          - wipe localStorage:
            - wipe localStorage
  - walkie-typie:
    - list:
    - text:
    - add:

  - blackboard:
    - News:

    - <__channelName__>:

    - list:

postgresql database:
  table:
    -
    -

javascript:

