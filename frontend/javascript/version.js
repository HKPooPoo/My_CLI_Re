/**
 * Platform & MOD API Version Constants
 * =================================================================
 * PLATFORM_VERSION — displayed in UI (blackboard-misc page).
 * MOD_API_VERSION  — the ctx.* API surface version.
 *   Templates can declare `minApiVersion` to warn on mismatch.
 * =================================================================
 */

export const PLATFORM_VERSION = '0.9.0';
export const MOD_API_VERSION = 1;
